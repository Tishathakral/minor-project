/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['assets.aceternity.com', 'image.lexica.art'], // Add the domain here
    },
};

export default nextConfig;
